# Automation QA Portfolio – Python, Pytest, Requests

Здравствуйте! Меня зовут **Дмитрий**, я **Automation QA Engineer**.  
Этот репозиторий — демонстрация моих практических навыков и подходов, которые я использую в повседневной работе.

---

## 🎯 Цели репозитория
- Показать структуру и организацию автотестов.
- Продемонстрировать подходы к тестированию **API**.
- Отразить практики, обеспечивающие стабильность и читаемость тестов.

---

## 🛠 Технологический стек

### Основной язык
- **Python 3.13.5**

### Инструменты и библиотеки
- **Pytest** — структура тестов, фикстуры, параметризация.
- **Requests** — API-тесты.

---

## 🧱 Структура проекта

Структура репозитория:

readme.md
.gitignore
Shop_API/
|___configs/
| |__ __init__.py
| |__config.py
|
|___fixtures/
| |__api.py
|
|___pages/
| |__ __init__.py
| |__base_page.py
| |__firm_page.py
|
|___tests/
| |__api/
| | |__test_firm.py
| |__files/
| | |__example_image.img
|
|___utils/
| |__ __init__.py
| |__api_auth.py
| |__endpoints.py
| |__request_logger.py
| |__signature.py
| |__token_access.txt
|
|___venv/
|___.env.v29
|___.gitlab-ci
|___conftest.py
|___pytest.ini
|___requirement.txt


---

## 🚀 Как запускать тесты

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
playwright install
```

### 2. Запуск всех тестов
```bash
pytest -vv
```

### 3. Запуск с Allure
```bash
pytest --alluredir=allure-results
allure serve allure-results
```

---

# 📊 Отчётность

- В проекте интегрирован Allure CLI, что позволяет:
  - Группировать тесты по фичам, историям, severity.
  - Отслеживать шаги, аттачи и параметры.
  - Анализировать падения через детальные отчёты.
- Пример команд для генерации:
```bash
allure generate allure-results -o allure-report --clean
```

---

# 🧩 Особенности подхода

- Разделение UI и API тестов по уровням.
- Использование паттерна Page Object для UI.
- Лёгкие и расширяемые API-клиенты.
- Продуманная система фикстур Pytest.
- Конфигурация через отдельный модуль.
- Читабельность и поддерживаемость как приоритет.

---

# 🔧 В разработке

- На данный момент идёт добавление:
  - CI (GitLab CI)
  - Docker-окружения

---

# 📬 Контакты

- Email: naydanov.dmitriy@gmail.com
- Telegram: @DatorDeus

---

# Далее вырезка из README рабочего проекта

---

# ⚙️ Полезные флаги и комбинации для запуска тестов в pytest

## 🔧 Основные флаги

| Флаг             | Назначение                                      | Пример               |
| ---------------- | ----------------------------------------------- | -------------------- |
| `-v`             | Подробный вывод (verbose)                       | `pytest -v`          |
| `-s`             | Не подавлять вывод `print()` / `stdout`         | `pytest -s`          |
| `-x`             | Остановить выполнение при первом упавшем тесте  | `pytest -x`          |
| `--maxfail=3`    | Остановить после 3 падений                      | `pytest --maxfail=3` |
| `-q`             | Тихий режим (меньше вывода)                     | `pytest -q`          |
| `-k "substring"` | Запуск тестов, имена которых содержат подстроку | `pytest -k "login"`  |
| `-m "tag"`       | Запуск тестов по маркеру                        | `pytest -m smoke`    |

---

## ⚡ Для ускорения и стабильности

| Флаг                       | Назначение                                           | Пример                            |
| -------------------------- | ---------------------------------------------------- | --------------------------------- |
| `-n auto` _(pytest-xdist)_ | Параллельный запуск тестов                           | `pytest -n auto`                  |
| `--dist loadscope`         | Делит тесты по фикстурам с сохранением session scope | `pytest -n auto --dist loadscope` |
| `--maxprocesses=4`         | Ограничить число процессов                           | `pytest -n 4`                     |
| `--lf`                     | Запустить только последние упавшие тесты             | `pytest --lf`                     |
| `--ff`                     | Запустить сначала упавшие в прошлый раз тесты        | `pytest --ff`                     |
| `--cache-clear`            | Очистить кэш pytest                                  | `pytest --cache-clear`            |

---

## 📊 Отчёты и логи

| Флаг                                 | Назначение                               | Пример                                            |
| ------------------------------------ | ---------------------------------------- | ------------------------------------------------- |
| `--html=report.html` _(pytest-html)_ | Генерация HTML-отчёта                    | `pytest --html=report.html --self-contained-html` |
| `--alluredir=allure-results`         | Сохранить результаты для Allure          | `pytest --alluredir=allure-results`               |
| `--junitxml=result.xml`              | Сохранить отчёт в формате JUnit (для CI) | `pytest --junitxml=result.xml`                    |
| `--log-cli-level=INFO`               | Вывод логов в консоль                    | `pytest --log-cli-level=INFO`                     |
| `--durations=10`                     | Показать 10 самых медленных тестов       | `pytest --durations=10`                           |

---

## 🧠 Отладка и диагностика

| Флаг             | Назначение                                  | Пример                  |
| ---------------- | ------------------------------------------- | ----------------------- |
| `--pdb`          | Войти в отладчик при падении                | `pytest --pdb`          |
| `--trace`        | Войти в отладчик до запуска теста           | `pytest --trace`        |
| `--maxfail=1 -x` | Остановиться на первой ошибке               | `pytest --maxfail=1 -x` |
| `--setup-show`   | Показать порядок и область действия фикстур | `pytest --setup-show`   |

---

## 🧩 Полезные комбинации

### 🔹 Быстрый и подробный запуск одного файла

```bash
pytest tests/test_login.py -v -s
```

### 🔹 Только последние упавшие тесты

```bash
pytest --lf -v
```

### 🔹 С HTML-отчётом и логами

```bash
pytest -v --html=report.html --self-contained-html --log-cli-level=INFO
```

### 🔹 Параллельный запуск smoke-тестов с Allure

```bash
pytest -m smoke -n auto --alluredir=allure-results
```

## 🧩 Рекомендации для Playwright / e2e тестов

- Избегай -n auto, если тесты делят один браузерный контекст.
- Используй --dist loadscope или отдельные session-level контексты.
- Добавь базовые настройки в pytest.ini:

```ini
[pytest]
addopts = -v -s --durations=5
markers =
    smoke: быстрые базовые проверки
    regression: регрессионные тесты
    slow: медленные e2e сценарии
```

---
